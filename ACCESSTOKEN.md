started the lecture on ACCESS TOKEN and REFRESH TOKEN 
diff ACCESS TOKEN and REFRESH TOKEN 
short live || long live  

user can login through the access token but we try to hit any endpoint throght which we can verify the refresh token that user have or that is present in the DB is same then new access token 